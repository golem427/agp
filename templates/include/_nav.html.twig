<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="nav-link active" aria-current="page" href="{{ path('home') }}"></a>
	<a class="navbar-brand" href="{{ path('home') }}"><img src="{{ asset('img/logo_ap3.png') }}" height="65rem" alt="logo Aguado Peinture"></img>
</a>
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
	<span class="navbar-toggler-icon"></span>
</button>
<div class="collapse navbar-collapse" id="navbarText">
	<ul class="navbar-nav mr-auto">
		<li class="nav-item active">
			<a class="nav-link" href="{{ path('home') }}">Accueil
				<span class="sr-only">(current)</span>
			</a>
			{# sr = "Screen reader” classes: They are now “visually hidden” classes.
						      Changed the Sass file from scss/helpers/_screenreaders.scss to scss/helpers/_visually-hidden.scss
						      Renamed .sr-only and .sr-only-focusable to .visually-hidden and .visually-hidden-focusable
						      Renamed sr-only() and sr-only-focusable() mixins to visually-hidden() and visually-hidden-focusable(). #}
		</li>
		<li class="nav-item active">
			<a class="nav-link" href="#">Réalisations</a>
		</li>
		<li class="nav-item active">
			<a class="nav-link" href="#">A propos</a>
		</li>
		<li class="nav-item active">
			<a class="nav-link" href="#">Contact</a>
		</li>
	</ul>
</div>
{# {% if app.user %}
				<div class="text-nowrap mx-auto">Bonjour
					<b>{{ app.user.firstname | capitalize }}</b>, vous êtes connecté en tant que :
					<span class="text-info">{{ app.user.roles | first }}</span>
				</div>
			{% endif %}  #}
{# 
{% if not app.user %}
	<a class="nav-link" href="{{ path('register_user') }}">Inscription</a>

	<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
{% endif %}

{% if app.user %}
	<div class="collapse navbar-collapse" id="navbarText">
		<ul class="navbar-nav ms-auto">

			{% if is_granted("ROLE_ADMIN") %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle text-primary" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Espace Admin
					</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						<li>
							<a class="dropdown-item" href="{{ path('show_users') }}">Gestion des inscrits</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('show_realisations') }}">Gestion des réalisations</a>
						</li>
					</ul>
				</li>

			{% endif %}
			<li class="nav-item">
				<a class="nav-link" href="{{ path('change_password') }}">Mon espace</a>
			</li>
			<li class="nav-item">
				<a class="nav-link text-danger" href="{{ path('app_logout') }}">
					<i class="fa-solid fa-right-from-bracket" color="red"></i>
				</a>
			</li>
		</div>
	{% endif %} #}
</ul></div></div></nav>
